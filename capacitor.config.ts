import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.alexhuman.meditstreak",
  appName: "Medit Streak",
  webDir: "out",
  ios: {
    contentInset: "automatic",
  },
};

export default config;
